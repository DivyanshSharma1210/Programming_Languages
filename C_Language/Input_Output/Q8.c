// Write a program to calculate the square of a given number.Number is entered by the User.

#include<stdio.h>

int main()
{
    int x;
    printf("Enter a Number : ");
    scanf("%d",&x);
    printf("Square of the Given Number %d is %d",x,x*x);
    printf("\n");
    return 0;
}