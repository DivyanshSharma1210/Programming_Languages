// Write a program to find the area of the circle.Take radius of corcle from user as an input.

#include<stdio.h>

int main()
{
    float radius;
    printf("Enter the radius of the Circle : ");
    scanf("%f",&radius);
    printf("Area of the Circle is : %f",3.14*radius*radius);
    printf("\n");
    return 0;
}